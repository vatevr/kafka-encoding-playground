# kafka-encoding-playground
Experimental java project to play around with kafka serializers and rate their performances


Run apache kafka with 

```sh
docker stack deploy --compose-file kafka.stack.yml kafka
```
